import { FsDocDirective } from './fs-doc.directive';

describe('FsDocDirective', () => {
  it('should create an instance', () => {
    const directive = new FsDocDirective();
    expect(directive).toBeTruthy();
  });
});
