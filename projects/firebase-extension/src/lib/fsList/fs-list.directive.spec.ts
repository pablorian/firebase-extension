import { FsListDirective } from './fs-list.directive';

describe('FsListDirective', () => {
  it('should create an instance', () => {
    const directive = new FsListDirective();
    expect(directive).toBeTruthy();
  });
});
